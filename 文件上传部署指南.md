# 文件上传功能部署指南

## 🚀 跨平台文件上传功能

本文件上传功能已优化，支持在Windows和Linux服务器上正常运行。

## 📋 功能特性

- ✅ 跨平台兼容（Windows/Linux）
- ✅ 自动目录创建和权限设置
- ✅ 配置化路径管理
- ✅ 文件类型验证
- ✅ 文件大小限制
- ✅ 按日期分目录存储
- ✅ 安全的文件名处理

## ⚙️ 配置说明

### 1. 基础配置（application.yml）

```yaml
# 文件上传配置
file:
  upload:
    path: uploads                  # 文件上传路径
    url-prefix: /api/files        # 文件访问URL前缀
    max-size: 800MB               # 最大文件大小
    allowed-types:                # 允许的文件类型
      - image/*
      - application/pdf
      - text/*
      - application/msword
      - application/vnd.openxmlformats-officedocument.*
      - audio/*
      - video/*
      - application/zip
      - application/x-rar-compressed
      - application/x-7z-compressed

spring:
  servlet:
    multipart:
      max-file-size: 800MB         # 单个文件最大大小
      max-request-size: 1GB        # 单次请求最大大小
      file-size-threshold: 50MB    # 临时文件阈值
      location: /tmp               # 临时文件存储位置
      enabled: true                # 启用文件上传
```

### 2. 路径配置选项

#### 相对路径（推荐）
```yaml
file:
  upload:
    path: uploads  # 相对于项目根目录
```

#### 绝对路径
```yaml
file:
  upload:
    path: /var/www/uploads  # Linux绝对路径
    # 或
    path: D:/uploads        # Windows绝对路径
```

## 🖥️ 不同环境部署

### Windows服务器部署

1. **路径配置**
   ```yaml
   file:
     upload:
       path: D:/myblog/uploads  # 使用绝对路径
   ```

2. **权限设置**
   - 确保应用有写入权限
   - 建议使用管理员权限运行

3. **测试命令**
   ```bash
   # 访问测试页面
   http://your-domain/test/upload
   
   # 测试文件系统
   http://your-domain/test/filesystem
   ```

### Linux服务器部署

1. **路径配置**
   ```yaml
   file:
     upload:
       path: /var/www/uploads  # 使用绝对路径
   ```

2. **权限设置**
   ```bash
   # 创建上传目录
   sudo mkdir -p /var/www/uploads
   
   # 设置权限
   sudo chown -R tomcat:tomcat /var/www/uploads
   sudo chmod -R 755 /var/www/uploads
   ```

3. **SELinux配置（如果启用）**
   ```bash
   # 设置SELinux上下文
   sudo setsebool -P httpd_can_network_connect 1
   sudo setsebool -P httpd_anon_write 1
   ```

4. **Nginx配置（如果使用）**
   ```nginx
   location /api/files/ {
       proxy_pass http://localhost:8080;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
   }
   ```

## 🔧 环境变量配置

### 生产环境配置

```bash
# 设置上传路径
export FILE_UPLOAD_PATH=/var/www/uploads

# 设置URL前缀
export FILE_UPLOAD_URL_PREFIX=/api/files

# 设置最大文件大小
export FILE_UPLOAD_MAX_SIZE=800MB
```

### Docker部署配置

```yaml
version: '3.8'
services:
  myblog:
    image: myblog:latest
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/app/uploads  # 挂载上传目录
    environment:
      - FILE_UPLOAD_PATH=/app/uploads
      - FILE_UPLOAD_URL_PREFIX=/api/files
```

## 🧪 测试功能

### 1. 系统环境测试
访问：`http://your-domain/test/filesystem`

测试内容：
- 操作系统信息
- 目录权限
- 文件读写权限
- 路径分隔符兼容性

### 2. 文件上传测试
访问：`http://your-domain/test/upload`

测试功能：
- 简单文件上传
- 批量文件上传
- 文件类型验证
- 文件大小限制

### 3. 文件管理测试
访问：`http://your-domain/admin/files`

测试功能：
- 文件列表显示
- 文件搜索
- 文件下载
- 文件删除

## 🚨 常见问题解决

### 1. 权限问题
**问题**：文件上传失败，提示权限不足
**解决**：
```bash
# Linux
sudo chown -R tomcat:tomcat /var/www/uploads
sudo chmod -R 755 /var/www/uploads

# Windows
# 右键文件夹 -> 属性 -> 安全 -> 编辑 -> 添加用户权限
```

### 2. 路径问题
**问题**：文件保存路径错误
**解决**：
- 检查配置文件中的路径设置
- 使用绝对路径避免相对路径问题
- 确保路径存在且有写入权限

### 3. 文件大小限制
**问题**：大文件上传失败
**解决**：
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 800MB
      max-request-size: 1GB
```

### 4. 跨域问题
**问题**：前端无法访问文件API
**解决**：
```java
@CrossOrigin(origins = "*")
@RestController
public class FileDownloadController {
    // 控制器代码
}
```

## 📊 监控和维护

### 1. 磁盘空间监控
```bash
# 检查上传目录大小
du -sh /var/www/uploads

# 定期清理临时文件
find /var/www/uploads -name "*.tmp" -mtime +7 -delete
```

### 2. 日志监控
```bash
# 查看文件上传日志
tail -f log/blog.log | grep "文件上传"

# 查看错误日志
tail -f log/blog.log | grep "ERROR"
```

### 3. 性能优化
- 定期清理过期文件
- 使用CDN加速文件访问
- 配置适当的缓存策略

## 🔒 安全建议

1. **文件类型限制**：只允许安全的文件类型
2. **文件大小限制**：防止恶意大文件上传
3. **路径安全**：防止路径遍历攻击
4. **权限控制**：限制文件访问权限
5. **定期清理**：定期清理无用文件

## 📞 技术支持

如果遇到问题，请：
1. 查看日志文件
2. 运行系统测试
3. 检查配置文件
4. 联系技术支持

---

**注意**：本功能已针对Windows和Linux服务器进行优化，确保跨平台兼容性。
