# 博客图片上传功能 - 跨平台部署指南

## 🚀 部署环境支持

本系统已优化支持以下部署环境：
- ✅ **宝塔面板** (Linux服务器)
- ✅ **Windows云服务器**
- ✅ **Docker容器**
- ✅ **传统Tomcat部署**
- ✅ **JAR包直接运行**

## 📋 部署前准备

### 1. 系统要求
- **Java**: JDK 8 或更高版本
- **数据库**: MySQL 5.7 或更高版本
- **内存**: 建议至少 1GB 可用内存
- **磁盘**: 建议至少 2GB 可用空间

### 2. 数据库准备
执行数据库升级脚本：
```sql
-- 执行 sql/blog_image_upload_upgrade.sql 中的SQL语句
source sql/blog_image_upload_upgrade.sql;
```

## 🏗️ 宝塔面板部署

### 1. 环境配置
```bash
# 创建网站目录
mkdir -p /www/wwwroot/yourdomain/uploads
mkdir -p /www/wwwlogs/myblog

# 设置权限
chmod 755 /www/wwwroot/yourdomain/uploads
chmod 755 /www/wwwlogs/myblog
```

### 2. 配置文件
使用 `application-baota.yml` 配置文件：
```yaml
file:
  upload:
    path: /www/wwwroot/yourdomain/uploads
```

### 3. 启动方式
```bash
# 方式1：使用启动脚本
chmod +x start_baota_server.sh
./start_baota_server.sh

# 方式2：手动启动
java -jar -Dspring.profiles.active=baota myblog.jar
```

### 4. 宝塔面板配置
- **端口**: 8080
- **运行目录**: `/www/wwwroot/yourdomain`
- **启动文件**: `myblog.jar`
- **环境变量**: `SPRING_PROFILES_ACTIVE=baota`

## 🖥️ Windows云服务器部署

### 1. 环境配置
```cmd
REM 创建目录
mkdir D:\webapps\uploads
mkdir D:\logs\myblog
mkdir C:\temp
```

### 2. 配置文件
使用 `application-windows.yml` 配置文件：
```yaml
file:
  upload:
    path: D:\webapps\uploads
```

### 3. 启动方式
```cmd
REM 方式1：使用启动脚本
start_windows_server.bat

REM 方式2：手动启动
java -jar -Dspring.profiles.active=windows myblog.jar
```

### 4. Windows服务配置
可以将应用注册为Windows服务，实现开机自启。

## 🐳 Docker部署

### 1. Dockerfile
```dockerfile
FROM openjdk:8-jre-alpine
VOLUME /tmp
COPY myblog.jar app.jar
RUN mkdir -p /app/uploads
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app.jar"]
```

### 2. 启动命令
```bash
docker run -d \
  --name myblog \
  -p 8080:8080 \
  -v /host/uploads:/app/uploads \
  -e SPRING_PROFILES_ACTIVE=docker \
  myblog:latest
```

## ⚙️ 环境变量配置

### 通用环境变量
```bash
# 数据库配置
DB_HOST=localhost
DB_NAME=myblog
DB_USER=root
DB_PASSWORD=your_password

# 文件上传配置
FILE_UPLOAD_PATH=/path/to/uploads

# 应用配置
SPRING_PROFILES_ACTIVE=production
```

### 宝塔面板环境变量
```bash
export FILE_UPLOAD_PATH=/www/wwwroot/yourdomain/uploads
export SPRING_PROFILES_ACTIVE=baota
export DB_PASSWORD=your_password
```

### Windows服务器环境变量
```cmd
set FILE_UPLOAD_PATH=D:\webapps\uploads
set SPRING_PROFILES_ACTIVE=windows
set DB_PASSWORD=your_password
```

## 🔧 故障排除

### 1. 文件上传失败
**问题**: 上传文件时提示"创建上传目录失败"
**解决方案**:
```bash
# 检查目录权限
ls -la /path/to/uploads

# 设置正确权限
chmod 755 /path/to/uploads
chown -R www-data:www-data /path/to/uploads
```

### 2. 图片无法访问
**问题**: 上传的图片无法通过URL访问
**解决方案**:
- 检查Nginx配置，确保静态文件访问正确
- 验证文件路径和URL映射
- 检查文件权限

### 3. 数据库连接失败
**问题**: 应用启动时数据库连接失败
**解决方案**:
- 检查数据库服务是否启动
- 验证数据库连接参数
- 确认数据库用户权限

### 4. 内存不足
**问题**: 应用运行缓慢或崩溃
**解决方案**:
```bash
# 增加JVM内存
java -Xms1g -Xmx2g -jar myblog.jar
```

## 📊 性能优化建议

### 1. 宝塔面板优化
- 启用Gzip压缩
- 配置CDN加速
- 优化MySQL配置
- 启用Redis缓存

### 2. Windows服务器优化
- 调整JVM参数
- 优化磁盘I/O
- 配置Windows防火墙
- 启用系统监控

### 3. 文件存储优化
- 使用SSD存储
- 定期清理临时文件
- 配置文件压缩
- 实施备份策略

## 🔒 安全配置

### 1. 文件上传安全
- 限制文件类型和大小
- 扫描恶意文件
- 使用安全的文件名
- 定期清理上传文件

### 2. 服务器安全
- 配置防火墙规则
- 定期更新系统补丁
- 使用HTTPS协议
- 限制管理员访问

## 📈 监控和维护

### 1. 日志监控
```bash
# 查看应用日志
tail -f /www/wwwlogs/myblog/blog.log

# 查看系统日志
journalctl -u myblog -f
```

### 2. 性能监控
- 监控CPU和内存使用
- 监控磁盘空间
- 监控网络流量
- 监控数据库性能

### 3. 定期维护
- 清理日志文件
- 备份数据库
- 更新应用版本
- 检查安全漏洞

## 🆘 技术支持

如果在部署过程中遇到问题，请检查：
1. 系统日志文件
2. 应用日志文件
3. 数据库连接状态
4. 文件权限设置
5. 网络连接状态

---

**部署完成后，您的博客系统将支持完整的图片上传功能，在任何环境下都能稳定运行！**
